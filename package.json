{
  "name": "webmaya-advanced-studio",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "npm run build && node --test tests/*.test.mjs"
  }
}
